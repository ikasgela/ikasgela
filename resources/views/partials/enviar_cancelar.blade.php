<div class="form-group">
    <button type="submit" class="btn btn-primary">{{ __('Send') }}</button>
    <a href="{!! ruta_memorizada() !!}" class="btn btn-link text-secondary">{{ __('Cancel') }}</a>
</div>
